# Pending — nightly cumulative manifest

> Regenerated by `/sync-docs` every nightly run. Cumulative from `base_release_tag`.

- **Base release tag:** `v2026.525.0`
- **Parent window:** `v2026.525.0` → parent `master` HEAD `8da50db`, effective cutoff `b754582` (24h quarantine)
- **Commits in window:** 6 (3 commits in quarantine, deferred to next run)
- **Doc-relevant manifest entries:** 3
- **Drift candidates:** 0 (check-drift.mjs against master)
- **Reconciliation candidates:** 0 (cumulative base is the release tag; all entries are net-new)
- **Genuine new dirs (rename-checked):** none (no adapter/plugin/skill dir changes in window)

## Auto-merge

_None._ No `.env.example` / config / env-schema changes in the window — the env-vars watcher matched nothing.

## PR-tier — applied this run

### 1. Resource memberships — join/leave projects & agents (`#6677`)

- **Watcher:** rest-routes, schemas, web-ui, server-services
- **Parent files:** `server/src/routes/resource-memberships.ts` (new); `server/src/services/resource-memberships.ts` (new); `packages/db/src/schema/{agent_memberships,project_memberships}.ts` (new); `packages/shared/src/{types,validators}/resource-memberships.ts` (new); `ui/src/components/MembershipAction.tsx`, `ui/src/hooks/useResourceMemberships.ts`, `ui/src/components/Sidebar{Projects,Agents}.tsx`.
- **Change kind:** added (new per-user follow/curate mechanism for sidebar visibility).
- **Docs targets:** new page `docs/reference/api/resource-memberships.md` (+ nav); `docs/guides/projects-workflow/projects.md` ("Keeping projects (and agents) in your sidebar").
- **Evidence:** Routes `GET /api/companies/:companyId/resource-memberships/me`, `PUT …/me/projects/:projectId`, `PUT …/me/agents/:agentId` with body `{ state: "joined" | "left" }`. Board-self auth only; activity log `resource_membership.joined`/`left`. Default-joined semantics in `defaultJoinedMap`.

### 2. Document annotations & comments (`#6733`)

- **Watcher:** rest-routes (folded into issues.ts), schemas, server-services, web-ui
- **Parent files:** `server/src/services/document-annotations.ts` (new); annotation routes added to `server/src/routes/issues.ts`; `packages/db/src/schema/document_annotation_{threads,comments,anchor_snapshots}.ts` (new); `packages/shared/src/{types,validators}/document-annotation.ts` (new); `ui/src/components/DocumentAnnotation{Layer,Panel}.tsx`, `IssueDocumentAnnotations.tsx`, `ui/src/lib/document-annotation-*.ts`.
- **Change kind:** added (anchored comment threads on issue documents, with revision re-anchoring).
- **Docs targets:** `docs/reference/api/issues.md` (new "Document Annotations" section); `docs/guides/day-to-day/issues.md` ("Annotating a document").
- **Evidence:** Routes under `/api/issues/:id/documents/:key/annotations[/:threadId[/comments]]` (GET/POST/PATCH). Create requires `baseRevisionId`/`baseRevisionNumber` + `selector` (quote+position) + `body`; stale revision → 409, bad selector → 422. Anchor states `active`/`shifted`/`orphaned` via `remapDocumentAnchor`. Activity: `issue.document_annotation_thread_created`/`comment_added`/`thread_resolved`/`thread_reopened`.

### 3. Bundled plugins in plugin manager (`#6734`)

- **Watcher:** rest-routes, web-ui
- **Parent files:** `server/src/routes/plugins.ts` (+161/-52); `ui/src/api/plugins.ts`; `ui/src/pages/PluginManager.tsx`.
- **Change kind:** modified (the Available Plugins list is now discovered from the checkout, includes vendored first-party plugins, and flags experimental ones).
- **Docs targets:** `docs/administration/plugins.md` (Available Plugins section + install button wording + quick-ref); `docs/reference/api/plugins.md` (`/examples` endpoint + new "Bundled plugin discovery" subsection).
- **Evidence:** `GET /api/plugins/examples` route path unchanged but now returns `discoverBundledPlugins()` output. Response type renamed `AvailablePluginExample → AvailableBundledPlugin`, adds `experimental: boolean`. `EXPERIMENTAL_BUNDLED_PLUGIN_PACKAGE_NAMES` = `@paperclipai/plugin-{llm-wiki,modal,workspace-diff}`. UI: `listBundled()`, `ExperimentalBadge`, "Install" button.

## Context-only (no direct edits)

- `a49afe5e` README license → Paperclip Labs, Inc, and `de367435` README brand alignment — docs-only on the parent README; both currently in 24h quarantine anyway.
- `8da50dbc` private-browser first-admin claim flow (`#6755`) — real feature (server routes, bootstrap UI) but **in quarantine** (~3h old at run time). Deferred to next nightly run.

## Screenshots

- No staleness detected: every entry in `docs/user-guides/screenshots/registry.json` has an empty `depends_on`, so UI changes can't be matched to screenshots yet. Populating `depends_on` is a standing maintenance gap, not a regression from this run. The plugin-manager, projects, agents, sidebar, and issue-document UI screenshots are the likely candidates to re-capture for these three features.

## Notes

- 3 of 6 cumulative parent commits processed; 3 are within the 24h quarantine (now=2026-05-28T10:13Z, cutoff=2026-05-27T10:13Z) — deferred to next nightly run.
- Reconciliation: none. Cumulative base is the release tag, so this manifest is entirely net-new entries with no disappeared prior entries.
