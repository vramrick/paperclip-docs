# Pending — nightly cumulative manifest

> Regenerated by `/sync-docs` every nightly run. Cumulative from `base_release_tag`.

- **Base release tag:** `v2026.529.0`
- **Parent window:** `v2026.529.0` → parent `master` HEAD `a800140`, effective settled cutoff `a4fa0ea` (24h quarantine)
- **Commits in cumulative window:** 119 total; 16 quarantined (younger than 24h, deferred to next run)
- **Doc-relevant manifest entries:** 3 applied this run; prior-run entries carried in cumulative window
- **Drift candidates:** 0 (`check-drift.mjs` against master — clean)
- **Reconciliation candidates:** 0 (nothing previously applied was reverted upstream)
- **Genuine new dirs (rename-checked):** `packages/teams-catalog/**` (new), `ui/src/pages/Artifacts.tsx` (new). The `skills/` → `.agents/skills/` move (maintainer skills `diagnose-why-work-stopped`, `paperclip-create-plugin`, `terminal-bench-loop`, plus new `create-issue-interaction-ui`) is a maintainer-tooling relocation, not a user-facing skill bundle — no doc impact. The canonical `skills/paperclip/**` bundle stays at `skills/`.

## ⏳ Quarantined this run (deferred to next run)

These landed within the last 24h, so they are held back until reverts have time to settle. Expect them in the next nightly manifest:

- **Information Architecture + project/agent visual refresh (experimental)** (`eaef47f4`, #7543) — large UI refresh across `ui/src/pages/Agents.tsx`, `AgentDetail.tsx`, project tiles/icons, `Sidebar`. Behind an experimental flag. Will invalidate many screenshots once settled.
- **Checkbox-confirmation issue interactions** (`4d5322c8`, #7649) + **issue interaction selector polish** (`bb880d99`, #7668) — `server/src/services/issue-thread-interactions.ts`, `ui/src/components/IssueThreadInteractionCard.tsx`.
- **Video artifact thumbnails** (`86253f52`, #7667) — extends the new Artifacts page (covered below) with inline video previews.
- **Guard git-sensitive adapter workspaces** (`7428fb95`, #7644) — `execution-workspace-policy`.
- **Prevent invalid agents from receiving assignments/runs** (`71a8464f`, #7663) — new `agent-assignability.ts` / `agent-invokability.ts` services + `packages/shared/src/agent-eligibility.ts`. User-visible: ineligible agents no longer get work. Candidate doc note next run.
- **Project icons** (`eaef47f4` + migration `0098_project_icon`) — projects can carry an icon.
- **CLI: send `X-Paperclip-Run-Id`** (`8b85fdfa`, #7642) — agents can mutate their issues via the CLI during a run. Minor; likely context-only.
- Dependency bumps (storybook, drizzle-kit, tailwindcss) + CI guards — not doc-relevant.

## Auto-merge

_None._ No `.env.example` / config / env-schema additions in the settled window — the env-vars watcher matched nothing.

## PR-tier — applied this run

### 1. Teams Catalog — installable team templates

- **Watcher:** skills-catalog (new sibling surface) / cli-commands / rest-routes
- **Parent files:** `packages/teams-catalog/**` (new package), `cli/src/commands/client/teams.ts` (new `teams` CLI command), `server/src/routes/teams-catalog.ts` (new routes), `packages/shared/src/types/teams-catalog.ts`, `packages/shared/src/validators/teams-catalog.ts`. (#7550, `fff3832a`)
- **Change kind:** added.
- **Surface:** A catalog of installable starter teams — `bundled` (always present: `company-defaults`, `product`, `software-development`) and `optional` (e.g. `content`) — each shipping agents, projects, tasks, and skills. Browse/inspect/preview/install via the new `teams` CLI command or the in-app Team Catalog page.
- **Docs targets:** `docs/reference/cli/teams.md` (new — CLI reference), `docs/guides/org/team-catalog.md` (new — guide), nav added under CLI and "Your Org & Agents".
- **Verification (Phase 5.5):** clean.

### 2. Company Artifacts page

- **Watcher:** server-services / rest-routes / web-ui
- **Parent files:** `server/src/services/company-artifacts.ts` (new), `packages/shared/src/types/artifact.ts` (new), `packages/shared/src/validators/artifact.ts` (new), `server/src/routes/companies.ts` (new `GET /:companyId/artifacts`), `ui/src/pages/Artifacts.tsx` (new). (#7621, `4693d770`)
- **Change kind:** added.
- **Surface:** A company-level view of every artifact agents produce — documents, attachments, and work products — filterable by media kind and groupable by task / parent task stacks, with inline previews.
- **Docs targets:** `docs/guides/day-to-day/artifacts.md` (new — guide), `docs/reference/api/companies.md` (new "List company artifacts" section), nav added under "Working Day-to-day".
- **Verification (Phase 5.5):** clean.

### 3. Low-trust review containment / trust presets

- **Watcher:** server-services / schemas
- **Parent files:** `packages/shared/src/trust-policy.ts` (new), `packages/shared/src/validators/trust-policy.ts` (new), `server/src/services/trust-preset-resolver.ts` (new), `server/src/services/source-trust.ts` (new), `server/src/services/low-trust-runtime-containment.ts` (new), migration `0097_low_trust_source_trust.sql`, schema changes on `issues` / `issue_comments` / `documents` / `issue_work_products`. (#7530, `dbebf30c`)
- **Change kind:** added.
- **Surface:** Trust presets (`standard`, `low_trust_review`) that contain less-trusted agents and sources at runtime (isolated workspace, sandbox driver, restricted secrets and tool classes) and quarantine their work products until they're reviewed and promoted.
- **Docs targets:** `docs/administration/trust-and-low-trust-review.md` (new — admin page), nav added under "Administration".
- **Verification (Phase 5.5):** clean.

## PR-tier — applied in prior runs (carried in cumulative window)

These remain in the cumulative diff from `v2026.529.0` and were authored in earlier nightly runs; no re-work needed:

1. **CLI API-parity handbook** — reference pages under `docs/reference/cli/`.
2. **`wireframe` + `last30days` catalog skills** — `docs/reference/skills.md`, `docs/guides/org/skills.md`.
3. **Routine scheduled-tick suppression while project is paused** — `docs/reference/api/routines.md`.
4. **Company archive cascade pauses agents** — `docs/administration/company.md`, `docs/reference/api/companies.md`.

## ⚠ Drift

_None._ `check-drift.mjs` against master found no documented surfaces missing upstream.

## ⚠ Reconcile

_None._ No previously-applied doc edit corresponds to a reverted parent commit.

## Screenshots

40 tracked captures are stale in the settled window (`AgentDetail.tsx`, `NewAgent.tsx`, `IssueDetail.tsx`, `Routines.tsx`). See `SCREENSHOTS_PENDING.md`. Recapture with `npm run screenshots:refresh` — PNGs land in a PR for human review, never auto-pushed.

## Orphans (not in nav — informational)

- `docs/reference/cli/commands.md`
- `docs/reference/cli/control-plane-commands.md`

Pre-existing; likely superseded by the per-command CLI handbook pages. Human decides whether to remove or register.
