# Pending — nightly cumulative manifest

> Regenerated by `/sync-docs` every nightly run. Cumulative from `base_release_tag`.

- **Base release tag:** `v2026.513.0`
- **Parent window:** `v2026.513.0` → `333a16b` (parent `master` HEAD `4c47eb4`; 9 commits younger than the 24h quarantine deferred to a future run)
- **Drift candidates:** 0
- **Reconciliation candidates:** 0
- **Manifest entries (doc-relevant):** 0

## Auto-merge

_None._

## PR-tier

_None._

## Notes

The two non-quarantined commits in this window are internal hardening / bugfix with no user-visible documented surface:

- `1bd44c8` — Cloudflare sandbox hardening (SSE keepalive heartbeat, internal bridge-channel stream handling, bumps the default `bridgeRequestTimeoutMs` from 30s to 300s, and updates the bridge template's `wrangler.jsonc` default `instance_type` from `lite` to `standard-2`). None of these defaults are currently quoted in our Cloudflare reference page, so no claim correction is needed. If we later document `bridgeRequestTimeoutMs` or the bridge-template wrangler defaults, those values will need to track parent.
- `333a16b` — Tolerates missing local heartbeat run-log files during company export so user comments still list when optional run-attribution metadata is absent. Internal resilience; not surfaced in export-import docs.

## Quarantined commits (will be evaluated in a future run)

- `901c088` propagate projectId into wakeup context
- `03ad5c5` issue document locking
- `dfcebf0` refresh issue documents from live updates
- `eb38b22` LLM Wiki package / migration validation
- `d5ba334` UI i18n runtime packages
- `7e1a27c` lockfile refresh
- `afb73ba` scale issue kanban board for high-volume columns
- `e2d7263` minimal i18next foundation
- `4c47eb4` multilingual issue preservation coverage
