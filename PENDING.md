# Pending — nightly cumulative manifest

> Regenerated by `/sync-docs` every nightly run. Cumulative from `base_release_tag`.

- **Base release tag:** `v2026.529.0`
- **Parent window:** `v2026.529.0` → parent `master` HEAD `a4fa0ea`, effective cutoff `e78c064` (24h quarantine)
- **Commits in window:** 72 effective (quarantined commits younger than 24h deferred to next run)
- **Doc-relevant manifest entries:** 3 new this run; 4 carried from prior runs
- **Drift candidates:** 0 (check-drift.mjs against master)
- **Reconciliation candidates:** 0 (nothing previously applied was reverted upstream)
- **Genuine new dirs (rename-checked):** none

## ⏳ Quarantined this run (deferred to next run)

These landed within the last 24h, so they are held back until reverts have time to settle. Expect them in the next nightly manifest:

- **Teams Catalog** (`fff3832a`, #7550) — new `packages/teams-catalog/**` package, `server/src/routes/teams-catalog.ts`, `ui/src/pages/TeamCatalog.tsx`. A brand-new product surface (installable team templates). Will need a new doc page next run.
- **Company artifacts page** (`4693d770`, #7621) — `server/src/services/company-artifacts.ts`, `ui/src/pages/Artifacts.tsx`.
- **Low-trust review containment** (`dbebf30c`, #7530) — `packages/shared/src/trust-policy.ts`, `server/src/services/source-trust.ts`, `trust-preset-resolver.ts`, `low-trust-runtime-containment.ts`, UI `TrustPresetSection`. New trust-presets feature.
- Comment redaction / deleted-comment cleanup, operator QoL (routines-by-project default, archived-routine hiding, inline video previews).

## Auto-merge

_None._ No `.env.example` / config / env-schema changes in the window — the env-vars watcher matched nothing.

## PR-tier — applied this run

### 1. `last30days` catalog skill + referenced (external-source) catalog entries

- **Watcher:** skills-catalog
- **Parent files:** `packages/skills-catalog/catalog/optional/research/last30days/catalog-ref.json` (new), `packages/skills-catalog/src/types.ts`, `packages/skills-catalog/src/catalog-builder.ts`, `packages/skills-catalog/generated/catalog.json` (modified).
- **Change kind:** added.
- **Surface:** The app-shipped catalog gained its first **referenced** entry — a catalog skill whose bytes are not bundled in the app but fetched from an external GitHub repo pinned to a commit (`CatalogSkill.source`, a `CatalogSkillGitHubSource`). `last30days` (optional, `research` category, `defaultInstall: false`) is sourced from `github.com/mvanhorn/last30days-skill` at `v3.3.0` / commit `daca71f`.
- **Docs targets:** `docs/reference/skills.md` (App-shipped catalog — new "Where a catalog skill's bytes come from" paragraph, `last30days` example, `source` field in the `CatalogSkill` inventory), `docs/guides/org/skills.md` (optional flavours + external-source note).
- **Verification (Phase 5.5):** clean.

### 2. Routine scheduled-tick suppression while project is paused

- **Watcher:** server-services
- **Parent files:** `server/src/services/routines.ts` (#7502, TON-2139).
- **Change kind:** modified.
- **Surface:** When a routine's project is paused, scheduled ticks no longer create work — they are recorded as a `skipped` run (`failureReason: "Skipped because the project is paused"`), the schedule advances, and the missed tick is not backfilled on resume.
- **Docs targets:** `docs/reference/api/routines.md` (run-status table `skipped` row + Practical Notes).
- **Verification (Phase 5.5):** clean.

### 3. Company archive cascade pauses agents

- **Watcher:** server-services / schemas
- **Parent files:** `server/src/services/companies.ts` (#7478), `packages/shared/src/constants.ts` (`PAUSE_REASONS` gained `company_archived`).
- **Change kind:** modified.
- **Surface:** Archiving a company now pauses every active agent (`pauseReason: "company_archived"`) and cancels their queued/in-flight wake-up requests, so an archived company stops doing background work. Non-destructive.
- **Docs targets:** `docs/administration/company.md` (Danger Zone), `docs/reference/api/companies.md` (Archive or delete).
- **Verification (Phase 5.5):** clean.

## PR-tier — applied in prior runs (carried in cumulative window)

These remain in the cumulative diff from `v2026.529.0` and were authored in earlier nightly runs; no re-work needed:

1. **CLI API-parity handbook** — 28 reference pages under `docs/reference/cli/` (window through `03e1e3ab`).
2. **`wireframe` bundled catalog skill** — `docs/reference/skills.md`, `docs/guides/org/skills.md`.
3. **OpenAPI document endpoint** — `docs/reference/api/overview.md`.
4. **Issue artifacts** (output playback, attachment streaming, skill upload) — `docs/reference/api/issues.md`, `docs/reference/skills.md`.

## ⚠ Drift

_None._

## ⚠ Reconcile

_None._

## Screenshots

10 stale entries flagged in `SCREENSHOTS_PENDING.md` (issue detail/list views, from the issue-artifacts UI changes). Recapture with `npm run screenshots:refresh` — PNGs go to a PR for human review, never auto-pushed.
