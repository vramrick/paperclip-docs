# Pending — nightly cumulative manifest

> Regenerated by `/sync-docs` every nightly run. Cumulative from `base_release_tag`.

- **Base release tag:** `v2026.529.0`
- **Parent window:** `v2026.529.0` → parent `master` HEAD `70b1a91`, effective cutoff `edeab22c` (24h quarantine)
- **Commits in window:** 30 effective (3 commits in quarantine, deferred to next run)
- **Doc-relevant manifest entries:** 3 new this run
- **Drift candidates:** 0 (check-drift.mjs against master)
- **Reconciliation candidates:** 0 (cumulative base is the release tag; nothing previously applied was reverted upstream)
- **Genuine new dirs (rename-checked):** none

## Auto-merge

_None._ No `.env.example` / config / env-schema changes in the window — the env-vars watcher matched nothing.

## PR-tier — applied this run

All three entries belong to one feature cluster: **agent-produced issue artifacts** (videos and files) that reviewers can see and play back directly on the board, including cloud reviewers who don't have access to the agent workspace.

### 1. Issue attachment streaming & artifact downloads

- **Watcher:** rest-routes
- **Parent files:** `server/src/routes/issues.ts`, `server/src/attachment-types.ts`
- **Change kind:** modified.
- **Surface:** Each attachment now exposes `openPath` and `downloadPath` alongside `contentPath`. The content route accepts `?download=1` to force a download disposition and supports HTTP **Range** requests (`206 Partial Content`, `Accept-Ranges`, `Content-Range`) so video can stream/seek. Default allowed upload types gain `application/zip`, `video/mp4`, `video/webm`, `video/quicktime`; generic `octet-stream` uploads get their video content-type inferred from the filename.
- **Docs targets:** `docs/reference/api/issues.md` (Attachments section).

### 2. Paperclip skill artifact-upload workflow

- **Watcher:** agent-skills
- **Parent files:** `skills/paperclip/references/artifacts.md` (new), `skills/paperclip/scripts/paperclip-upload-artifact.sh` (new), `skills/paperclip/SKILL.md` (modified).
- **Change kind:** added.
- **Surface:** The bundled Paperclip skill now tells agents to upload user-inspectable output files to the current issue (as attachment-backed artifact work products) instead of leaving local workspace paths behind. A bundled `paperclip-upload-artifact.sh` helper does the upload and prints issue-safe markdown links.
- **Docs targets:** `docs/reference/skills.md`.

### 3. Issue Output section + rich attachment previews

- **Watcher:** web-ui
- **Parent files:** `ui/src/pages/IssueDetail.tsx`, `ui/src/components/IssueAttachmentsSection.tsx`, `ui/src/components/issue-output/*`.
- **Change kind:** modified.
- **Surface:** The issue detail view gains an **Output** section that plays agent-produced video inline and renders rich previews for file deliverables; attachments expose distinct open vs. download actions.
- **Docs targets:** `docs/guides/day-to-day/issues.md`.
- **Screenshots:** 10 stale (see `SCREENSHOTS_PENDING.md`) — all entries tracking `ui/src/pages/IssueDetail.tsx`.

## Considered, not documented

- **`.github/**` commitperclip PR quality & security gates** — parent-repo CI infrastructure, not a Paperclip product surface.
- **`scripts/dev-runner*` snapshot race fix** — dev tooling.
- **`ui` StandaloneBrowserControls + pwa-display-mode** — PWA chrome for installed/cloud viewing; screenshot-adjacent only, folded into the Output narrative rather than given its own copy.

## ⚠ Anchor-map gap

- **`packages/skills-catalog/**` is unwatched.** A new bundled **wireframe** product skill landed in the skills catalog (`packages/skills-catalog/catalog/bundled/product/wireframe/**`), but no watcher globs `packages/skills-catalog/**` — the `agent-skills` watcher only globs `skills/**`. The prior run manually routed catalog changes to `docs/reference/skills.md` + `docs/guides/org/skills.md`, so the doc target exists; the map just doesn't auto-detect it. **Action:** add/extend a watcher so future catalog skills are caught.

## ⚠ Drift

_None._

## ↻ Renames detected

_None._

## In quarantine (deferred to next run, <24h old at run time)

`70b1a91` Improve CLI API parity coverage (#6626) — large CLI/API parity push (new `cli/src/commands/client/*` command groups + `server/src/routes/openapi.ts`) · `68401f8` Remove linked-issue gate from commitperclip (#7423) · `d58a862` fix(issues): coerce anchor.createdAt to Date before postgres binding (#5220)
