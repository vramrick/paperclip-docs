# Pending — nightly cumulative manifest

> Regenerated by `/sync-docs` every nightly run. Cumulative from `base_release_tag`.

- **Base release tag:** `v2026.517.0`
- **Parent window:** `v2026.517.0` → parent `master` HEAD `e3c875c`, effective cutoff `c91a062` (24h quarantine)
- **Commits in window:** 21 (8 commits in quarantine, deferred to next run)
- **Doc-relevant manifest entries:** 14
- **Drift candidates:** 0 (check-drift.mjs against master)
- **Reconciliation candidates:** 0 (all 13 prior entries still in cumulative diff)
- **Genuine new dirs (rename-checked):** `packages/plugins/sandbox-providers/modal`

## Auto-merge

_None._ The env-vars watcher matched only `vitest.config.ts` files (false positives from `packages/**/config.ts` glob) — skipped.

## PR-tier

### 1. New Modal sandbox provider plugin (`#6245`, `#6290`)

- **Watcher:** adapters (sandbox-providers)
- **Parent files:** `packages/plugins/sandbox-providers/modal/**` (9 added)
- **Change kind:** added — genuine new dir, confirmed by `detect-renames.mjs`
- **Docs targets:** new page `docs/reference/adapters/modal.md` (already exists from prior nav stub); mention in `docs/reference/adapters/sandbox-providers.md`; mirror an existing sandbox-provider page as a structure neighbour.
- **Evidence:** New `packages/plugins/sandbox-providers/modal/{plugin.ts,manifest.ts,worker.ts,README.md}`. README documents install (`@paperclipai/plugin-modal`), config fields (`appName`, `image`, `tokenId`, `tokenSecret`, `environment`, `workdir`, `sandboxTimeoutMs`, `idleTimeoutMs`, `execTimeoutMs`, `blockNetwork`, `cidrAllowlist`, `reuseLease`), reuse semantics (no native pause — `reuseLease: true` keeps billing running), and Node 22+ vendor support note (Node 20 best-effort).

### 2. `worktree init --force` no longer wipes repo worktrees (`#6240`)

- **Watcher:** cli-commands
- **Parent files:** `cli/src/commands/worktree.ts` (+6/-1)
- **Change kind:** modified (behaviour fix)
- **Docs targets:** `docs/reference/cli/commands.md`, `docs/reference/cli/setup-commands.md` — wherever `paperclipai worktree init --force` is described.
- **Evidence:** The `--force` branch of `runWorktreeInit` now removes only `paths.configPath` and `paths.envPath` instead of recursively removing the whole `<repo>/.paperclip/` directory. `worktrees/` checkouts survive.

### 3. Routine env runtime contract (`#6212`)

- **Watcher:** schemas, server-services (routines, secrets), shared-validators
- **Parent files:** `packages/db/src/migrations/0086_routine_env_runtime_contract.sql` (new); `packages/db/src/schema/routines.ts`; `packages/shared/src/validators/routine.ts`; `packages/shared/src/validators/secret.ts`; `packages/shared/src/types/routine.ts`; `server/src/services/routines.ts`; `server/src/services/secrets.ts`; `server/src/__tests__/qa-routine-secrets-e2e.test.ts` (new).
- **Change kind:** added (new env column + revision-pinned runs)
- **Docs targets:** `docs/how-to/create-a-daily-routine.md`, `docs/reference/api/secrets.md`, `docs/reference/deploy/secrets.md`.
- **Evidence:** Migration adds `routines.env jsonb` and `routine_runs.routine_revision_id uuid` (FK → `routine_revisions.id`, SET NULL on delete) plus `routine_runs_revision_idx`. Validator + service work wires routine env to secret bindings.

### 4. New workspace diff viewer plugin (`#6071`) + polish (`#6383`)

- **Watcher:** plugin-sdk (new bundled plugin under `packages/plugins/plugin-workspace-diff/`)
- **Parent files:** `packages/plugins/plugin-workspace-diff/**` (18 added; further refined in `#6383`)
- **Change kind:** added — new first-party plugin; subsequent polish for controls + base-ref + sticky headers
- **Docs targets:** `docs/reference/plugins/workspace-diff.md` (exists; polish from `#6383` applied this run — Controls section, view fallback, sticky toolbar).
- **Evidence:** New plugin package with `manifest.ts`, `worker.ts`, `src/ui/index.tsx`, `src/workspace-diff.ts`, plus comprehensive tests. `#6383` adds split/unified toggle, `defaultBaseRef` prefill, sticky toolbar (`sticky top-0 z-30` in `src/ui/index.tsx`), and `head → working-tree` fallback when no base ref is set.

### 5. Plugin SDK surface changes (`#6261`, plus rolling refactor)

- **Watcher:** plugin-sdk
- **Parent files:** `packages/plugins/sdk/src/{host-client-factory,index,protocol,testing,types,worker-rpc-host}.ts`; parent `doc/plugins/PLUGIN_AUTHORING_GUIDE.md`, `doc/plugins/PLUGIN_SPEC.md`, `doc/plugins/LOCAL_PLUGIN_DEVELOPMENT.md`.
- **Change kind:** modified (public surface drift + new "managed resources" authoring concept)
- **Docs targets:** `docs/reference/plugins/sdk.md`, `docs/how-to/develop-a-plugin-locally.md`.
- **Evidence:** Six SDK source files touched; parent's PLUGIN_AUTHORING_GUIDE.md was updated for managed resources. Audit exported types/functions from `sdk/src/index.ts` against our reference page.

### 6. `environmentProbe` RPC timeout raised to 120s (`#6289`)

- **Watcher:** plugin-sdk
- **Parent files:** plugin SDK / host (cold-start sandboxes — Modal in particular)
- **Change kind:** modified (timeout default)
- **Docs targets:** any page that quotes a probe-timeout default (search before editing). If none, this is context-only.
- **Evidence:** Commit `e5a0f5de` — "raise environmentProbe RPC timeout to 120s for cold-start sandboxes."

### 7. SecretBindingPicker wired into JsonSchemaForm `secret-ref` fields (`#6339`)

- **Watcher:** web-ui
- **Parent files:** `ui/src/components/JsonSchemaForm.tsx`, `ui/src/components/IssueProperties.tsx`, several plugin pages.
- **Change kind:** modified (user-visible UX — any JSON schema form field marked `format: secret-ref` now renders a binding picker).
- **Docs targets:** `docs/reference/api/secrets.md` if it mentions `secret-ref` schemas; plugin SDK reference if `secret-ref` is part of the manifest contract.

### 8. Provider vault secrets UX — AWS discovery + vault removal (`#6381`)

- **Watcher:** web-ui, rest-routes, server-services (secrets)
- **Parent files:** `server/src/routes/secrets.ts` (+ new `POST /api/companies/:companyId/secret-provider-configs/discovery/preview` endpoint); `server/src/services/secrets.ts`; `packages/shared/src/validators/secret.ts`; `ui/src/pages/Secrets.tsx`; `ui/src/pages/Secrets.render.test.tsx`.
- **Change kind:** added (new endpoint + UX) and modified (removal flow polish)
- **Docs targets:** `docs/reference/api/secrets.md` (document the new `/secret-provider-configs/discovery/preview` endpoint and the existing `/secret-provider-configs/*` family which is currently undocumented); `docs/reference/deploy/secrets.md` (operator guidance for AWS Secrets Manager provider vault setup); `docs/administration/` (Secrets page UX for vault discovery prefill + removal confirmation).
- **Evidence:** New route at `/companies/:companyId/secret-provider-configs/discovery/preview` with `secretProviderConfigDiscoveryPreviewSchema`. Provider-vault DELETE returns `404 Provider vault not found` on missing IDs. Storybook fixtures + dark+light screenshots captured in parent PR #6381.

### 9. Guard cheap recovery model usage (`#6371`)

- **Watcher:** server-services (recovery), schemas
- **Parent files:** `server/src/services/recovery/{model-profile-hint,run-liveness-continuations,service,successful-run-handoff}.ts` (+ tests); `server/src/services/heartbeat.ts`; `server/src/services/issues.ts`; `server/src/services/productivity-review.ts`.
- **Change kind:** modified (recovery-path invariant: cheap model profile only repairs status/liveness, never propagates to source-work continuations).
- **Docs targets:** wherever recovery / cheap-model behaviour is described (search `docs/administration/`, `docs/reference/`). If currently undocumented, this is context-only — the invariant is operator-visible only when configured.
- **Evidence:** New `model-profile-hint.test.ts` and updates to `successful-run-handoff.ts` guard against cheap-profile hints following deliverable mutations.

### 10. Hide sandbox-provider plugins from Instance Settings sidebar (`#6341`)

- **Watcher:** web-ui
- **Parent files:** `ui/src/components/Sidebar*.tsx`, `ui/src/pages/PluginManager.tsx`.
- **Change kind:** modified (sidebar UX — sandbox-provider-only plugins no longer appear as their own sidebar rows; they already redirect to the Environments page).
- **Docs targets:** `docs/administration/plugins.md` if it describes per-plugin sidebar entries (a quick scan suggests not). Affects any screenshot that shows Settings sidebar with a sandbox-provider plugin listed.

### 11. Cloud tenant import without browser origin (`#6378`)

- **Watcher:** server-services
- **Parent files:** board-mutation guard / cloud tenant import path.
- **Change kind:** modified (CSRF exemption for trusted `source: "cloud_tenant"` actors).
- **Docs targets:** none — internal trust boundary. Context-only.

### 12. UI + dev-ops quality-of-life polish (`#6384`)

- **Watcher:** web-ui
- **Parent files:** many `ui/src/components/*.tsx`, `ui/src/pages/*.tsx`, `ui/src/components/DevRestartBanner.tsx`, `ui/src/components/InstanceSidebar.tsx`, `ui/src/components/SidebarAccountMenu.tsx`, `ui/src/components/MissingPluginTabPlaceholder.tsx`, etc.
- **Change kind:** modified (cosmetic + dev-ops affordances — relaxed 44px touch targets for small inline widgets, new dev restart banner, placeholder for missing plugin tabs).
- **Docs targets:** none in particular — context-only. May invalidate screenshots when `depends_on` gets populated.

### 13. Remove planning badge from inbox issue rows (`#6269`, PAP-9691) + textarea/autocomplete fixes (`#6222`, `#6311`)

- **Watcher:** web-ui
- **Parent files:** `ui/src/components/IssueRow.tsx`, `IssuesList.tsx`, `ui/src/components/NewIssueDialog.tsx`, `ui/src/context/EditorAutocompleteContext.tsx`, `ui/src/pages/Secrets.tsx`, `ui/src/components/ui/textarea.tsx`.
- **Change kind:** modified (small UX fixes).
- **Docs targets:** none directly. Manual-review flag for any user-guide screenshots showing affected components.

### 14. Runtime control-plane fixes (`#6380`)

- **Watcher:** server-services, rest-routes
- **Parent files:** `server/src/http/body-limits.ts` (new); `server/src/routes/company-import-paths.ts` (new); `server/src/routes/{companies,issues}.ts`; `server/src/services/{issues,issue-thread-interactions,plugin-lifecycle,plugin-worker-manager}.ts`; `server/src/middleware/error-handler.ts`; `server/src/app.ts`; `packages/db/src/backup-lib.ts`.
- **Change kind:** modified (correctness fixes — issue list pagination + scheduled retry comment handling, hot-reload re-applies pending plugin migrations, plugin-schema worktree seed restore, public tenant DB startup hardening, stale request-confirmation expiry after user comments, feedback export shutdown hardening, plugin worker `error` event listener guard).
- **Docs targets:** none directly — overwhelmingly internal correctness. One operator-visible constant introduced: `PORTABLE_JSON_BODY_LIMIT = 64mb` on portable import endpoints (vs `DEFAULT_JSON_BODY_LIMIT = 10mb` elsewhere). Worth a one-line note in `docs/reference/api/companies.md` or `docs/reference/deploy/limits.md` if/when those describe request size limits, but the constants are hard-coded (not env-tunable) so this is low priority. Otherwise context-only.
- **Evidence:** New `server/src/http/body-limits.ts` defines the two limits; new `server/src/routes/company-import-paths.ts` centralises the `/api/companies/import` route path. Commit message at `c91a062` enumerates each fix.

## Context-only (no direct edits)

- `chore(lockfile)` (`#6263`), Docker copy fix (`#6338`), `[codex] Roll up May 17 branch changes` (`#6210`) — internal/CI.
- Heartbeat service tests churn — many `server/src/__tests__/heartbeat-*` files updated. No documented contract change in window beyond the routine-revision wiring already captured in entry 3.
- Plugin-llm-wiki internal refinements (`ui/app.tsx`, `wiki/core.ts`) — review separately before touching `docs/reference/plugins/llm-wiki.md`.

## Edits applied this run

- `docs/reference/plugins/workspace-diff.md` — added "Controls in the Changes tab" subsection covering split/unified toggle, working-tree vs against-ref toggle, base-ref input with `defaultBaseRef` prefill, sticky toolbar. Also noted the `head → working-tree` fallback when no base ref is set. Verified against `packages/plugins/plugin-workspace-diff/src/ui/index.tsx` at parent `f2575305`.

## Notes

- 21 of 29 cumulative parent commits processed; 8 are within 24h quarantine (now=2026-05-23T08:31Z, cutoff=2026-05-22T08:31Z) — deferred to next nightly run. Quarantined commits include `#6386` (agent permissions plan), `#6547`/`#6548`/`#6549` (plugin runtime hardening + cloud upstream sync + import reliability), `#6550` (mobile board polish), `#6423` (wrapped-company issue prefix conflicts), `#6433` (invite page query-key fix), `#6560` (E2B sandbox upload/lease fix).
- Anchor-map mismatch found: watcher `db-migrations` lists `packages/db/migrations/**` but actual parent path is `packages/db/src/migrations/**`. Surface in next maintenance pass.
- Env-vars watcher matched two `vitest.config.ts` files — false positives from `packages/**/config.ts` glob. Consider tightening the watcher (e.g. `packages/*/src/config.ts`) or adding an exclude for `vitest.config.ts`.
- Rename detector reports `packages/plugins/sandbox-providers/modal` as a genuinely new dir (not a rename).

## Plugin SDK followups

<!-- TODO Items below were flagged during the SDK audit against parent c0c5a82 but could not be
fully verified against the current parent source in this pass. Spot-check before acting. -->

- TODO: `PluginRoutinesClient` is defined in `packages/plugins/sdk/src/types.ts` but not re-exported from `packages/plugins/sdk/src/index.ts`, while `ctx.routines` is reachable on `PluginContext`. Decide whether to add the type to the SDK export list upstream, or document the asymmetry permanently.
- TODO: `Routine` and `RoutineRun` are imported by the SDK from `@paperclipai/shared` and used in `PluginRoutinesClient` return types but are not re-exported from the SDK index. Plugin authors typing these returns must import from `@paperclipai/shared` directly — confirm and document if intentional.
- TODO: Verify whether any pre-managed-resources symbol names (e.g. older managed-agent helpers) were renamed in the `c0c5a82..f2575305` window. The current audit only spot-checked `index.ts` exports against the existing `sdk.md` symbol list.
