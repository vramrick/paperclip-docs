# Pending — nightly cumulative manifest

> Regenerated by `/sync-docs` every nightly run. Cumulative from `base_release_tag`.

- **Base release tag:** `v2026.529.0`
- **Parent window:** `v2026.529.0` → parent `master` HEAD `1227bb8`, effective cutoff `03e1e3ab` (24h quarantine)
- **Commits in window:** 34 effective (quarantined commits younger than 24h deferred to next run)
- **Doc-relevant manifest entries:** 3 new this run
- **Drift candidates:** 0 (check-drift.mjs against master)
- **Reconciliation candidates:** 0 (cumulative base is the release tag; nothing previously applied was reverted upstream)
- **Genuine new dirs (rename-checked):** none

## Auto-merge

_None._ No `.env.example` / config / env-schema changes in the window — the env-vars watcher matched nothing.

## PR-tier — applied this run

### 1. CLI API-parity coverage (#6626) — full CLI reference handbook

- **Watcher:** cli-commands
- **Parent files:** `cli/src/index.ts` + `cli/src/commands/client/*.ts` (12 new command groups: `project`, `goal`, `cost`, `token`, `connect`, `prompt`, `run`, `workspace`, `adapter`, `asset`, `skill`, `routine-api`, plus the large `access` group; modifications across `activity`, `agent`, `approval`, `auth`, `cloud`, `company`, `context`, `dashboard`, `feedback`, `issue`, `plugin`, `secrets`, `configure`, `worktree`).
- **Change kind:** added + modified.
- **Surface:** Parent shipped a large CLI/API-parity push — full client command coverage for the control plane from the terminal. This was in **quarantine** the previous run (commit `70b1a91`, <24h old then) and became eligible this run.
- **Decision (with maintainer):** Adopt the comprehensive, pre-written CLI handbook from `paperclip-cli/cli-documentation` as the **per-command reference**, replacing the single-page `commands.md` monolith. The guide/runbook track (operating-model, autonomous-operation, recipes, AI-operator) is **deferred** to a later expansion. No new top-level tier — everything stays under **Reference → CLI**.
- **Docs targets (28 pages under `docs/reference/cli/`):** `overview`, `installation`, `common-options`, `output-and-scripting`, `setup-commands`, `dev-environments`, `authentication` (auth/token/connect/context), `company`, `project`, `goal`, `issue`, `agent`, `prompt`, `run`, `routine`, `adapter`, `approval`, `activity`, `dashboard`, `cost`, `workspace`, `access`, `skills`, `secrets`, `cloud`, `plugin`, `asset`, `feedback`.
- **Retired:** `docs/reference/cli/commands.md` and `docs/reference/cli/control-plane-commands.md` → converted to "moved" tombstones + route redirects in `site/redirects.json` (`reference/cli/commands` & `…/control-plane-commands` → `reference/cli/overview`).
- **Nav:** `site/content.json` CLI section rebuilt to the 28-page list.
- **Inbound link fixups:** `docs/reference/skills.md`, `docs/how-to/sync-to-cloud-upstream.md` repointed off the retired pages.
- **Verification (Phase 5.5):** all new command pages verify clean against parent. One verify-edit "unverified" on `access.md:95 --claim-secret` is a **tooling false negative** — parent declares it via `.requiredOption(...)` (cli/src/commands/client/access.ts:197), which `verify-edit.mjs` does not scan (it only greps `.option(`). The doc claim is correct.

### 2. `wireframe` app-shipped catalog skill

- **Watcher:** skills-catalog (newly added watcher — see note below)
- **Parent files:** `packages/skills-catalog/catalog/bundled/product/wireframe/SKILL.md` (new), `packages/skills-catalog/generated/catalog.json` (modified).
- **Change kind:** added.
- **Surface:** A new **bundled**, `product`-category catalog skill that produces low-fidelity black-and-white UI wireframes as standalone SVG files (recommended for `designer`/`product`/`engineer`; `defaultInstall: false`). Flagged as an anchor-map gap last run; the `skills-catalog` watcher has since been added, so it was caught automatically this run.
- **Docs targets:** `docs/reference/skills.md` (App-shipped catalog), `docs/guides/org/skills.md` (catalog flavours).

### 3. OpenAPI document endpoint

- **Watcher:** rest-routes
- **Parent files:** `server/src/routes/openapi.ts` (new).
- **Change kind:** added.
- **Surface:** The server now publishes a machine-readable OpenAPI document at `GET /api/openapi.json` (also fetchable via `paperclipai access openapi`).
- **Docs targets:** `docs/reference/api/overview.md` (new "OpenAPI Specification" subsection). CLI side covered by the new `access` page.

## Considered, not documented

- **`server/src/services/*` changes** (board-auth, issues, plugin-loader, plugin-tool-dispatcher, routines) — internal/behavioural; no user-visible surface shift in the eligible window (the routines pause feature lands in a quarantined commit — see below).
- **`ui/src/components/StandaloneBrowserControls`, `pwa-display-mode`, issue-output internals** — already folded into the issue-artifacts narrative documented in the prior run (`docs/guides/day-to-day/issues.md`, `docs/reference/api/issues.md`).
- **`.github/**`, dependabot, dep bumps** — repo CI/infra, not a product surface.

## ⚠ Drift

_None._

## ↻ Renames detected

_None._

## In quarantine (deferred to next run, <24h old at run time)

- `d60f50e4` **feat(routines): suppress scheduled ticks while project is paused (TON-2139)** — user-facing routine behaviour; document next run once it clears quarantine.
- `bc0a076e` fix(ui): stop linking foreign tracker keys as Paperclip issues · `1227bb8e` Improve OpenAPI spec coverage and auth metadata · `244a5b80` plugins: allow agent JWTs to access plugin tool endpoints · `93206f73` stop archived companies from waking agents · `62863126` plugin-tool-dispatcher fix · plus dependabot/dep-bump commits.
