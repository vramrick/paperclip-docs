# Screenshots Pending — v2026.517.0

> Regenerated by `/sync-docs` on release run for `v2026.517.0`.

## Staleness detection: not actionable this run

All 274 entries in `docs/user-guides/screenshots/registry.json` have empty `depends_on` arrays, so the staleness check is a no-op. This is tracked in [`maintenance/follow-ups.md`](maintenance/follow-ups.md) under "Screenshot anchor population".

## UI surfaces that changed visually in v2026.517.0

These surfaces had material UI changes in this release. If you have screenshots of them in the registry, populate `depends_on` so the next sync flags them automatically — and consider re-capturing now:

- **Kanban board view** of the Issues page — new density (`auto` / `comfortable` / `compact`), cold-lane mode (`auto` / `expanded` / `collapsed`), and column page-size controls. Source: `ui/src/components/KanbanBoard.tsx`, `ui/src/components/IssuesList.tsx`.
- **Issue documents section** — lock indicator on the document header, "redirected to derived document" state when an agent writes into a locked document, and live-refresh behaviour on create/update/restore/delete. Source: `ui/src/components/IssueDocumentsSection.tsx`, `ui/src/context/LiveUpdatesProvider.tsx`.
- **Issue detail page** general layout — touched alongside the documents section. Source: `ui/src/pages/IssueDetail.tsx`.

## Earlier carry-over surfaces (still no screenshots)

From the previous release window, still pending capture:

- **Blocked Inbox tab** (`/inbox/blocked`).
- **Issue recovery action card** on issue detail.
- **Sibling navigation footer** on issue detail.
