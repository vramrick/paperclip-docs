# Screenshots Pending — nightly (parent `master` → `b754582`)

> Regenerated by `/sync-docs` on the nightly run cumulative from `v2026.525.0`.

## Staleness detection: not actionable this run

All 274 entries in `docs/user-guides/screenshots/registry.json` have empty `depends_on` arrays, so the staleness check is a no-op. This is a standing maintenance gap, not a regression from this run.

## UI surfaces that changed visually in this window

These surfaces had material UI changes in the processed window (`#6677`, `#6733`, `#6734`). If you have screenshots of them in the registry, populate `depends_on` so the next sync flags them automatically — and consider re-capturing now:

- **Plugin Manager** — Available Plugins now lists discovered bundled plugins (examples + first-party) with a **Bundled** badge and an **Experimental** badge on maturing first-party plugins; install button now reads **Install**. Source: `ui/src/pages/PluginManager.tsx`.
- **Project & Agent sidebars** — new **Join** / **Leave** controls for curating which projects and agents appear in your sidebar. Source: `ui/src/components/SidebarProjects.tsx`, `ui/src/components/SidebarAgents.tsx`, `ui/src/components/MembershipAction.tsx`.
- **Issue document annotations** — selectable-text annotation layer, an annotations panel with open/resolved threads, and inline highlight markers on documents. Source: `ui/src/components/DocumentAnnotationLayer.tsx`, `ui/src/components/DocumentAnnotationPanel.tsx`, `ui/src/components/IssueDocumentAnnotations.tsx`, `ui/src/components/IssueDocumentsSection.tsx`.
- **Projects / Agents list pages** — touched alongside the membership work. Source: `ui/src/pages/Projects.tsx`, `ui/src/pages/Agents.tsx`, `ui/src/pages/ProjectDetail.tsx`, `ui/src/pages/AgentDetail.tsx`.

## Carry-over surfaces (still no screenshots)

Standing capture backlog from earlier windows:

- **Blocked Inbox tab** (`/inbox/blocked`).
- **Issue recovery action card** on issue detail.
- **Sibling navigation footer** on issue detail.
