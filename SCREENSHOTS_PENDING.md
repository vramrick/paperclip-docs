# Screenshots Pending — v2026.513.0

> Regenerated by `/sync-docs` on release run for `v2026.513.0`.

## Staleness detection: not actionable this run

All 274 entries in `docs/user-guides/screenshots/registry.json` have empty `depends_on` arrays, so the staleness check is a no-op. This is tracked in [`maintenance/follow-ups.md`](maintenance/follow-ups.md) under "Screenshot anchor population".

## New UI surfaces introduced in v2026.513.0 (no screenshots yet)

The release adds genuinely new dashboard surfaces. Whoever captures these should add light + dark variants and register them in `registry.json`:

- **Blocked Inbox tab** (`/inbox/blocked`) — list view + per-row chip variants (`Needs decision`, `Blocked chain stalled`, `Needs attention`, `Recovery required`, `External wait`, `Owner paused`). Source: `ui/src/components/BlockedInboxView.tsx`, `ui/src/components/BlockedReasonChip.tsx`.
- **Issue recovery action card** — appears on issue detail surfaces when a recovery action is active. Source: `ui/src/components/IssueRecoveryActionCard.tsx`.
- **Sibling navigation footer** — previous/next buttons on issue detail. Source: `ui/src/components/IssueSiblingNavigation.tsx`.

## Touched UI components (existing screenshots may need re-capture)

These existing dashboard surfaces had related code changes; spot-check the registered screenshots if any cover them:

- `ui/src/components/IssueRow.tsx` (recovery indicator added)
- `ui/src/components/IssueLinkQuicklook.tsx` (blur/click race fix)
- `ui/src/components/ActiveAgentsPanel.tsx` (recovery indicator added)
- `ui/src/components/IssueChatThread.tsx`
- `ui/src/pages/Inbox.tsx` (tab integration)
- `ui/src/pages/IssueDetail.tsx`
